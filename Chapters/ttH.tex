
\subsection{Cross-section predictions for $t\bar t H$}
\label{sec:results-xsects-tth}
New predictions for the $t\bar t H$ total cross section have recently appeared in Ref.~\cite{Balsach:2025jcw}. These results
combine predictions accurate at NNLO in QCD~\cite{Catani:2022mfv,Devoto:2024nhl} together with soft-gluon resummation up to NNLL~\cite{Kulesza:2015vda,Broggio:2015lya,Broggio:2016lfj,
Kulesza:2017ukk,Kulesza:2018tqz,Broggio:2019ewu,Kulesza:2020nfh}, and include all effects of EW origin up to NLO~\cite{Frixione:2014qaa,Frixione:2015zaa,Frederix:2018nkq}.
Soft-gluon resummation is carried out in two independent frameworks (SCET and dQCD), which makes it possible to obtain a conservative estimate of missing higher-order
uncertainties via scale variations. In Ref.~\cite{Balsach:2025jcw}, a thorough estimate of many sources of theoretical uncertainties is carried out.
The only one which may be phenomenologically relevant (besides scale variations, PDF and $\alphas$ uncertainties) is the one stemming 
from the approximation of the two-loop virtual amplitude carried out in order to perform the NNLO computation. Such an uncertainty amounts to
\begin{equation}
    \Delta_{\rm virt} = 0.9\%\,
\end{equation}
for the numbers presented here. The total theory uncertainty, $\Delta_{\rm th}$, is computed by combining in quadrature this effect together with scale variations. Following Ref.~\cite{Balsach:2025jcw}, the central value of the renormalisation
and factorisation scales is $\mu_R=\mu_F=m_t+m_H/2$.
The values for the total cross sections at 13, 13.6, and 14 TeV can be found in Tables~\ref{tab:tth-tot13},~\ref{tab:tth-tot136}, and~\ref{tab:tth-tot14}, respectively. Cross sections
for the mass values
\begin{equation}
    m_H \in [ 124.6, 125., 125.09, 125.38, 125.6, 126.] \rm GeV\,,
\end{equation}
have been computed. All other values, inside and outside this range, have been obtained
from the linear formula
\begin{equation}
    \sigma(m_H)=\sigma(124.6) +\frac{\sigma(126)-\sigma(124.6)}{1.4}(m_H-124.6)\,,
    \label{eq:inttth}
\end{equation}
which interpolates for $124.6 < m_H < 126$~GeV and extrapolates outside that interval.
The cross sections at the directly computed mass points carry a residual Monte Carlo
uncertainty at the level of a few per mille, which Eq.~\eqref{eq:inttth} does not smooth;
it is visible as a mild non-monotonicity in $m_H$ of some of the tabulated predictions,
and is negligible compared with the quoted uncertainties.
%Other values have been obtained via linear interpolation, using the formula
%\begin{equation}
%    \sigma(m_H)=\sigma(124.6) +\frac{\sigma(126)-\sigma(124.6)}{1.4}(m_H-124.6)\,. 
%    \label{eq:inttth}
%\end{equation}
For other energies, we advise employing the numbers
presented in Ref.~\cite{Karlberg:2024zxx}.



\subsection{Cross-section predictions for $t H$}
\label{sec:results-xsects-singleth}
In this section, we provide predictions for $tH$ production. 
For this process, three main production mechanisms concur: 
$t$-channel, $s$-channel and $tWH$ associated production. Their characteristics are very similar to those of single-top (without Higgs) production processes. As is the case
for these processes, at LHC energies the $t$-channel mode is by far the dominant one. At variance with $t\bar tH$, where the cross section
is proportional to the square of the top-quark Yukawa, for $tH$ it breaks down into three terms: a term independent of $y_t$, a linear term and a quadratic
term. As such, $tH$ production is sensitive to the sign of $y_t$. 

State-of-the-art predictions
for these processes include corrections up to NLO QCD~\cite{Demartin:2015uha,Demartin:2016axk} as well as NLO EW~\cite{Pagani:2020mov}. When
the latter are considered, however, interferences between the three production channels cannot be neglected, and the corresponding breakdown 
therefore becomes ill defined. Thus, the impact of EW corrections can only be assessed
by considering all the channels together (possibly imposing the selection cuts which enhance a given channel). In this case, the effect on the inclusive
cross section has been found in Ref.~\cite{Pagani:2020mov} to be about $-3.5\%$. Given the rather low rate of these processes, together with
the issues related to their inclusion when keeping the three channels separate, EW
corrections are not included in the reference cross sections.\\

All single-top and Higgs cross-sections presented below are computed with {\sc MadGraph5\_aMC@NLO}~\cite{Alwall:2014hca,Frederix:2018nkq}. 

The central value $\mu$ of the renormalisation and factorisation scales is set in a process-dependent manner:
\begin{itemize}
    \item $tH$ ($t$ channel): $\mu= \frac{m_H+m_t}{4}\,$, both for the computation in the 4FS and in the 5FS;
    \item $tH$ ($s$ channel): $\mu= \frac{m_H+m_t}{2}\,$;
    \item $tWH$: $\mu= \frac{m_H+m_t+m_W}{2}\,$;
\end{itemize}
All numbers are computed in the five-flavour scheme. For the $t$-channel production mechanism, the uncertainty $\Delta_{\rm scale+\textrm{FS}}$
is obtained as the envelope of the scale variations for the
five- and four-flavour scheme computation, both at NLO. For the 4FS prediction, we employ $m_b=4.92$~GeV.\\
For $tWH$, contributions belonging to $t\bar t H$ entering at NLO via real emissions are removed using the so-called ``Diagram removal with interference'' 
scheme (known as DR2), as implemented in {\sc MadGraph5\_aMC@NLO}, see Refs.~\cite{Demartin:2016axk,Frixione:2019fxg}.

Tables~\ref{tab:th-t-tot13},~\ref{tab:th-t-tot136},~\ref{tab:th-t-tot14};~\ref{tab:th-t-top13},~\ref{tab:th-t-top136},~\ref{tab:th-t-top14}; and~\ref{tab:th-t-atop13},~\ref{tab:th-t-atop136},~\ref{tab:th-t-atop14}
show predictions for $t$-channel production of $tH\,+\,\bar tH$ at $\sqrt{s}=$13, 13.6 and 14 TeV respectively, $tH$ production alone, and $\bar tH$ production alone at the same energies. Predictions for 
$s$ channel production are organised similary in 
Tables~\ref{tab:th-s-tot13},~\ref{tab:th-s-tot136},~\ref{tab:th-s-tot14};~\ref{tab:th-s-top13},~\ref{tab:th-s-top136},~\ref{tab:th-s-top14}; and~\ref{tab:th-s-atop13},~\ref{tab:th-s-atop136},~\ref{tab:th-s-atop14}. 
Finally, Tables~\ref{tab:thw-tot13},~\ref{tab:thw-tot136}, and~\ref{tab:thw-tot14}
show predictions for $tW^-H \,+ \,\bar t W^+H$ associated production (the cross section of the two separate processes are equal to each other in this case) at the three energies.
As for $t\bar t H$, cross sections
for the mass values
\begin{equation}
    m_H \in [ 124.6, 125., 125.09, 125.38, 125.6, 126.] \rm GeV\,,
\end{equation}
have been computed. All other values have been obtained from Eq.~\eqref{eq:inttth},
which interpolates within the computed range and extrapolates outside it.
%Other values have been obtained via linear interpolation using Eq.~\ref{eq:inttth}.



